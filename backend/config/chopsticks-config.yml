# chopsticks-astar.yml
# Chopsticks configuration for Astar network

# Use HTTPS endpoint instead of WebSocket to avoid message size limits
endpoint: https://polkadot.api.onfinality.io/public
port: 8546
mock-signature-host: true
